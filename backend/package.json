{
  "name": "nfl-backend",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node migrate-and-start.js",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:generate": "prisma generate",
    "postinstall": "chmod +x node_modules/.bin/prisma && node_modules/.bin/prisma generate || true"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "prisma": "^5.22.0"
  },
  "devDependencies": {}
}
